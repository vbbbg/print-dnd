{
    "buildCommand": "pnpm build:web",
    "outputDirectory": "apps/print-editor/dist-app",
    "framework": "vite",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}